import React from "react";
import Header from "../Header";
import Table from "../Table";

const Wrapper = () => {
    return (
        <div>
            <Header />
            <Table />
        </div>
    );
}

export default Wrapper;